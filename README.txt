Aekus Singh Trehan - No partner
All steps for running the code follows the same steps as the automated tests
Go to any file in the test-resources directory (other than the TestingTools file) and run it

Analysis of the code and project runtime and space complexity is in the analysis pdf along with the reasoning for key decisions that were made 

To run the GUI, you need to have a runtime configuration with JavaFX installed, then you can run the ProjectApp file to see the GUI
